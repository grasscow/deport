%p#notice= notice

%p
  %b Name:
  = @product.name
%p
  %b Description:
  = @product.description
%p
  %b Price:
  = @product.price
%p
  %b Weight:
  = @product.weight
%p
  %b category:
  = @product.category
-if @product.image?
  = link_to @product.image.url do
    = image_tag @product.image.url(:medium)
= link_to 'Edit', edit_product_path(@product)

= link_to 'Back', products_path
-#  создаем кнопку похожую на кнопку #(product_id:@product.id) чтобы передавался и айди продукта на создание пункта корзины
= link_to line_items_path(product_id: @product.id), data: {method: :post, remote: true},class: "btn btn-primary" do
  =fa_icon "cart-plus 2x"
  -#cart-plus 2x больше иконки cart-plus в 2 раза
  Добавить в корзину
